/*import ShopContent from '../contents/ShopContent.js';
export default class ShopManager{
    constructor(){
        //this.uiScene=uiScene;

        this.currentView=null;
        //this.shopContent=new ShopContent(uiScene,this);
    }
    openShelf(shelfData){
        if(this.currentView)this.currentView.destroy();

        //this.currentView=this.shopContent.createView(shelfData);

        this.currentView.setPosition(
            this.uiScene.scale.width/2,
            this.uiScene.scale.height/2,
        );

        //this.currentView.setPosition(centerX,centerY);
    }
    closeShelf(){
        if(this.currentView){
            this.currentView.destroy();
            this.currentView=null;
        }
    }
}*/